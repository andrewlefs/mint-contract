import { CompilerContext } from "../context";
import { SrcInfo, AstId } from "../grammar/ast";
import { TypeRef } from "./types";
export type StatementContext = {
    root: SrcInfo;
    returns: TypeRef;
    vars: Map<string, TypeRef>;
    requiredFields: string[];
};
export declare function emptyContext(root: SrcInfo, returns: TypeRef): StatementContext;
export declare function isLvalue(path: AstId[], ctx: CompilerContext): boolean;
export declare function resolveStatements(ctx: CompilerContext): CompilerContext;
